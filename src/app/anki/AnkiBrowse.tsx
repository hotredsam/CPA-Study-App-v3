// fidelity/deferred: Browse view (search + filter all cards, bulk edit, CSV import/export) — Night 7 scope

import { Card } from '@/components/ui'

export function AnkiBrowse() {
  return (
    <Card>
      <div className="flex items-center justify-center py-16 text-center">
        <div>
          <p className="text-lg font-semibold text-[color:var(--ink)] mb-2">Browse</p>
          <p className="text-sm text-[color:var(--ink-faint)]">Coming soon</p>
        </div>
      </div>
    </Card>
  )
}
